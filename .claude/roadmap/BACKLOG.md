# Backlog

Deferred work and captured ideas. Newest first. An idea recorded here is not a commitment.

Format: `| ID | Item | Why it matters | Size | Status |` — sizes per `workflow/DEVELOPMENT_WORKFLOW.md`.

| ID | Item | Why it matters | Size | Status |
|---|---|---|---|---|
| B-002 | Bootstrap the app scaffold (Vite + React + TS strict + AntD + Vanilla Extract + TanStack Router/Query + Zustand + Zod + Vitest) once the product overview arrives | Phase 2 cannot start without it; deliberately deferred so structure follows requirements, not the reverse | M | Blocked on Phase 1 |
| B-001 | Add ESLint/Prettier/tsconfig enforcing `standards/frontend.md` mechanically (import boundaries, no `any`, no relative cross-folder imports) | Machine-enforced standards do not drift; review-enforced ones do | S | Open |

## Idea capture (Phase 0 rule)

Application ideas surfacing before the product overview are recorded here as notes only — never implemented. See `CLAUDE.md` §9.

| Idea | Source | Date |
|---|---|---|
| _(none yet)_ | | |
